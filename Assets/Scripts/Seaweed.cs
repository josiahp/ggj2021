using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Seaweed : MonoBehaviour
{
    // Start is called before the first frame update

    private BillboardAsset seaweed;
    void Start()
    {
        seaweed = new BillboardAsset();
    }
}
